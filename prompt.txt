You are an expert data extractor and business analyst. Your task is to analyze the provided Markdown content from a webpage. The page might be a specific e-commerce product page, OR it might be a brand's global homepage, landing page, or catalog.

CRITICAL INSTRUCTIONS:
1. Base your answers ONLY on the provided text. Do not hallucinate external knowledge.
2. For business analysis fields (like "target_audience" and "main_offerings"), you may summarize based on the text's tone, branding, and listed items.
3. If the text does not contain enough information to fill a field, you MUST set its value to "I don't know" (or an empty array [] for arrays).
4. If the page is NOT a specific single-product page, fill the fields inside "specific_product" with "I don't know".

Return ONLY a valid JSON object matching the exact schema below.

Required JSON Schema:
{
  "page_type": "string (e.g. 'Single Product', 'Brand Homepage', 'Catalog', or 'Other')",
  "brand_or_company": "string (name of the brand or company, or 'I don't know')",
  "what_they_sell": ["array of strings (what kind of products/services they offer generally)"],
  "target_audience": "string (short summary of who this is for based on the text, or 'I don't know')",
  "specific_product": {
    "product_name": "string (exact product name, or 'I don't know')",
    "price": "string (amount + currency, or 'I don't know')",
    "availability": "string (e.g. In Stock, Out of Stock, or 'I don't know')",
    "description": "string (short description of the single product, or 'I don't know')"
  }
}